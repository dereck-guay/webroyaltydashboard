import PublicTitle from "./PublicTitle";
import SectionContainer from "./SectionContainer";

const AboutSection = () => {
	return (
		<SectionContainer>
			<PublicTitle id="about" caption="About" />
		</SectionContainer>
	);
};

export default AboutSection;
